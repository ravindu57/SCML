{
  "name": "scml-client",
  "version": "1.0.0",
  "description": "SCML (Secure Context Mediation Layer) client — trust-aware mediation for agentic AI and RAG systems. Zero runtime dependencies.",
  "license": "MIT",
  "keywords": [
    "llm",
    "security",
    "prompt-injection",
    "rag",
    "agent",
    "middleware",
    "scml",
    "trust-mediator"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "test": "node --test test/*.test.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
