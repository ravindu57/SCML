apiVersion: v1
kind: Service
metadata:
  name: trust-mediator
  namespace: trust-mediator
  labels:
    app.kubernetes.io/name: trust-mediator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trust-mediator
  ports:
    - name: http
      port: 80
      targetPort: http
