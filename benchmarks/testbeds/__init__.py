"""
Evaluation testbeds (PRD §14.1).

Implemented:
  - memory_poisoning — purpose-built, derived from published memory-injection
    and backdoor attack patterns; measures the §6.5 memory integrity layer,
    TrustMediator's primary contribution.

Planned (PRD §14.1, not yet implemented — do not cite as existing):
  - agentdojo   — multi-step agent tasks with indirect injection in tool data
  - injecagent  — single-turn tool-output injection
"""
