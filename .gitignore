# Strategy notes, not source. Patent positioning, valuation ranges and a frank
# assessment of the project's gaps — none of which should travel with the code.
# Repository visibility changes; this file staying local does not.
# Delete this line to track it.
INDUSTRY_READINESS_AND_IP_PLAN.md

# Secrets & local state — never commit
.env
*.db
*.sqlite3

# Python
__pycache__/
*.py[cod]
.venv
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Build artifacts (setuptools + the TypeScript client's compiled output and
# packed tarball — `npm pack` runs `prepack`, so dist/ is always regenerated)
build/
dist/
*.tgz

# Node
node_modules/

# Runtime artifacts
.logs/

# Build / LaTeX / personal files — not source
*.aux
*.log
*.out
*.toc
*.pdf
*.zip
*.png
missfont.log
SCML_Project_Report.*
SCML_Project_Source.zip
TrustMediator_Architecture*.png
.claude/.Rhistory
k8s/.Rhistory
Terminal Commands/
scml results/

# Claude Code personal overrides (team settings in .claude/settings.json ARE committed)
.claude/settings.local.json

# MCP server (local only, not public)
trust_mediator/mcp/

# Standalone MCP server bundle (packed live in scml-mcp-standalone repo)
scml-mcp-bundle/
scml-mcp-bundle.mcpb

# Console history
*.Rhistory

# Exhibition launcher runtime state (pids, logs)
.exhibition/

# run-demo.sh runtime state (pids, logs, merged policy)
.demo/
